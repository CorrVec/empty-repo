name: Issues
on:
  pull_request:
      branches:
      - main
jobs:
  print_issue_title:
    runs-on: ubuntu-latest
    name: Print pull request title
    steps:
    - run: echo "${{github.event.pull_request.title}}"
